package com.ebank.bankservice.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MailConfig {
    // Configuration mail déjà définie dans application.yml
    // Ce bean est prêt pour une extension future (OTP, notifications, etc.)
}
